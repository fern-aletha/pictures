
export const ONE_SECOND = 1000;

export const SECONDS_BEFORE_END = 10;

export const TYPES = Object.freeze({
  MANY: 'M', // Many (time limit for an entire quiz)
  ONE: 'A', // One (the time limit for a questions page)
} as const);
